@model Sitecore.Data.Items.Item

<div class="container" style="page-break-after:always; min-height:400px;">
    <div class="row">
        <div class="col-sm">
            <h1><a name="HelixDetails">Helix Details</a></h1>
            <p>
                @Html.Raw(Model.Fields["Helix Details"].Value)
            </p>   
         </div>
    </div>
</div>